<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PokeDex" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorOnPrimary">@color/light_text_primary</item>
        <item name="android:textColorSecondary">@color/light_text_primary</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnSecondary">@color/light_accent</item>
    </style>

    <style name="Theme.PokeDex" parent="Base.Theme.PokeDex" />

</resources>
